services:
  - type: web
    name: vita-mahjong-multiplayer
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx tsx server/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5050
